package pe.innvovamind.zendriver.ui.settings.data.local

import androidx.room.Dao
import androidx.room.Query

@Dao
interface SettingsDao {
//    @Query("SELECT * FROM driverprofile join driver on driverprofile.driver_id = driver.id JOIN users on driver.user_id = users.id")
//    fun getSettings(): List<SettingsEntity>

}